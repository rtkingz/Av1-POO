# Pedir um número inteiro ao usuário
numero = int(input("Digite um número inteiro: "))
# Verificar se o número é primo
if numero <= 1: #pra verificar se o numero tá sendo igual ou menor que 1 ( não primo )
    print(f"{numero} não é primo.")
else:
    for i in range(2, numero): #loop em 2 pra verificar se o numero é divisivel por 2 ou nao
        if numero % i == 0:
            print(f"{numero} não é primo.")
            break
    else:
        print(f"{numero} é primo.")